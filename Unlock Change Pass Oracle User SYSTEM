
ALTER USER SYSTEM IDENTIFIED BY "x_ByIxNpeamII9zw";

ALTER USER SYSTEM IDENTIFIED BY "x_ByIxNpedfII9zw" ACCOUNT UNLOCK;



Use the following commands for deactivation:

ALTER PROFILE DEFAULT LIMIT PASSWORD_VERIFY_FUNCTION NULL;

ALTER PROFILE SAPUPROF LIMIT PASSWORD_VERIFY_FUNCTION NULL;

After finishing the patch, use the following commands to activate the verification:

ALTER PROFILE DEFAULT LIMIT PASSWORD_VERIFY_FUNCTION vwfsstig01;

ALTER PROFILE SAPUPROF LIMIT PASSWORD_VERIFY_FUNCTION vwfsstig01;

